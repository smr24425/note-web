## MODIFIED Requirements

### Requirement: 長按觸發選單
手機模式下，長按筆記卡片 SHALL 觸發操作選單（等同桌面右鍵選單），並提供震動回饋。

#### Scenario: 長按觸發選單
- **WHEN** 手機使用者長按筆記卡片超過 500ms
- **THEN** 顯示操作選單（置頂、移至資料夾、刪除），並在支援的裝置上觸發輕微震動（10ms）

#### Scenario: 移動手指不觸發
- **WHEN** 手機使用者按住後移動手指（滑動列表）
- **THEN** 不觸發長按選單
